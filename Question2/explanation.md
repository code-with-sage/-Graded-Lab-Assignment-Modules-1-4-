1. cd ~ <br>
   mkdir documents<br>
    these commands will create a new directory name documents firstly cd ~ will bring us to home <br>
    directory and mkdir will create a directory named documents

2. cd documents<br>
   touch plan.txt<br>
     firstly cd will bring us in documents directory then touch command will create a text file name
     plan.txt

3. echo "today is friday and im making assignment" > plan.txt<br>
    echo command will print the provide text and > will same the wtriten text in plan.txt file 

4. ls -l plan.txt<br>
    ls command will -l for plan.txt will display Di the permissions and ownership details along with some additional informations. 

5. cp plan.txt plan_copy.txt<br>
    cp command is use for copy one files this command will create another file with same content as plan.txt and new file name will be plan_copy.txt

6. mv documents project_documents<br>
     here mv command will more the ducumets directory contect to project_documents here we are using this command for renaming the directory 

7. cd project_documents<br>
   mkdir archive<br>
     cd cammand will bring us to project_documents directory and mkdir command will create a directory name archive inside project_document directory 

8. mv plan_copy.txt archive<br>
      this command will move plan_copy.txt file form project_document directry to archive directory

9. ls -R project_documents<br>
      ls with optuion -R will list all the files and directry inside project_document directory recursively 

10. realpath plan_copy.txt<br>
      realpath command will showcase the absolute path of plan_copy.txt file 

   
